export interface FindCustomAppointmentScheduleDTO {
  date?: string; // The date for which to find the custom appointment schedule
  hour?: string; // The hour for which to find the custom appointment schedule
}
